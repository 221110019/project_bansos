import 'package:flutter/material.dart';

class TambahStokOwner extends StatelessWidget {
  const TambahStokOwner({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: const Center(
          child: Text("Ini halaman tambah barang"),
        ),
      ),
    );
  }
}
